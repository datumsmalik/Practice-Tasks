# main.py
#Utils is a collection of small Python functions and classes which make common patterns shorter and easier.
from utils import greet   #utils is the name of a Python file (module) named utils.py, and greet is a function defined inside it.

print(greet("World"))    #greet is a func name 







# Build the Image  (docker build -t copy-demo .)
# Run the Container (docker run --rm copy-demo)   # -rm container ko remove krta he after exit 